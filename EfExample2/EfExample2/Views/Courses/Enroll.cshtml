@model EfExample2.Models.Course

@{
    ViewBag.Title = "Enroll";
}

<h2>Enroll</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Select student to enroll in @Model.Name</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

    @*<div class="form-group">
        <label asp-for="Students" class="control-label"></label>
        <select asp-for="Students" class="form-control " asp-items="@(new SelectList(ViewBag.Items,"Value", "Text"))"></select>
        <span asp-validation-for="Students" class="text-danger"></span>
    </div>*@
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
  
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
